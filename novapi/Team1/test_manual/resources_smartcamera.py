






smart_camera_1.detect_sign(1)

smart_camera_1.detect_sign_location(1, "middle // up // down // left // right")


smart_camera_1.get_sign_x(1)
smart_camera_1.get_sign_y(1)
smart_camera_1.get_sign_width(1)
smart_camera_1.get_sign_height(1)


# Autocode

# Detect cube (yellow color)
# Find xy coordinate of cube (supopse is center is (0,0))
# Move the bot but bot angle always perpendicular to arena's angle
# (bot angle === arena_angle)
# (Move to cube)